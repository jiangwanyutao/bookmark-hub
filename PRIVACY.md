# 书签体检隐私政策

最后更新：2026 年 9 月 15 日

书签体检（以下简称「本扩展」）是一个开源的浏览器书签管理扩展，源代码公开在 <https://github.com/jiangwanyutao/bookmark-hub>。本扩展**没有服务器，不收集、不上传、不出售任何用户数据**。本政策说明本扩展会接触哪些数据、这些数据保存在哪里、在什么情况下会离开你的设备。

## 1. 本扩展读取和保存的数据

| 数据 | 用途 | 保存位置 |
|---|---|---|
| 书签（标题、网址、目录结构、添加时间） | 浏览、搜索、检查、清理和整理书签 | 浏览器自带的书签系统 |
| 扫描结果（每个网址能否打开、状态码、跳转地址、检查时间） | 显示失效、重定向、待确认的书签 | 本机 IndexedDB |
| 操作记录与恢复点（批量操作前后的书签树副本） | 撤销操作、从恢复点恢复 | 本机 IndexedDB |
| 书签标签 | 按标签搜索书签 | 本机 IndexedDB |
| 需要 VPN 的网站列表、主题设置 | 扫描判定与界面偏好 | 本机 IndexedDB / localStorage |
| AI 服务配置（Base URL、API Key、模型名、发送范围） | 调用你自己配置的 AI 服务 | 本机 `storage.local`，不随浏览器同步 |

以上数据只保存在你的浏览器里。卸载本扩展时，浏览器会一并删除本扩展保存的本地数据（书签本身除外，书签属于浏览器）。

## 2. 数据在什么情况下会离开你的设备

本扩展只在以下两种**由你主动触发**的情况下发出网络请求：

### 2.1 健康扫描

当你点击「扫描书签」时，本扩展会向书签里的网址逐个发送请求，检查网页能否打开。

- 请求**不携带 Cookie 等登录信息**（`credentials: omit`）。
- 内网地址（如 `localhost`、`192.168.x.x`）和网址里带登录凭据的链接**直接跳过，不会被请求**。
- 为了识别「页面其实是找不到」，本扩展最多读取网页开头 64 KB 以提取标题，读取内容不保存、不外传。
- 这些请求直接发往书签对应的网站，本扩展开发者无法看到。

### 2.2 AI 智能整理与 AI 标签

只有在你**自行配置了 AI 服务**并点击「开始整理」或「生成标签」时，本扩展才会把书签信息发送给**你填写的那个 AI 服务地址**。

- 发送范围由你在设置里选择：仅标题 / 标题 + 域名（默认）/ 标题 + 网址（会去掉网址里的登录凭据类参数）。
- 内网地址的书签一律不发送。
- API Key 只发送给你填写的地址。
- AI 服务如何处理这些数据，受该服务提供方自己的隐私政策约束；本扩展开发者无法访问这些请求。

除上述两种情况外，本扩展不发出任何网络请求，不使用任何统计、分析、广告或崩溃上报服务，也不加载远程代码。

## 3. 浏览器权限说明

| 权限 | 用途 |
|---|---|
| `bookmarks` | 读取和修改书签 |
| `storage`、`unlimitedStorage` | 在本机保存扫描结果、操作记录、恢复点、标签和 AI 配置 |
| `favicon` | 显示浏览器已缓存的网站图标，不发网络请求 |
| `search` | 在「书签导航」里按回车时，用浏览器默认搜索引擎搜索 |
| `webRequest` 与访问所有网站（可选） | **首次点击「扫描书签」时才请求**，用于检查网址能否打开、识别跳转和网络错误；拒绝后除扫描外的功能照常可用 |
| 访问 AI 服务地址（可选） | 保存 AI 配置时请求，只针对你填写的那个地址 |

## 4. 浏览器同步

如果你开启了浏览器的书签同步，本扩展对书签的删除、移动和编辑会通过浏览器自己的同步功能同步到你的其他设备。这是浏览器的行为，本扩展不参与传输。本扩展保存的扫描结果、操作记录和 AI 配置不会同步。

## 5. 儿童隐私

本扩展不面向 13 岁以下儿童，也不收集任何人的个人信息。

## 6. 政策变更

如果本扩展的数据处理方式发生变化，会更新本页面并修改顶部的「最后更新」日期。

## 7. 联系方式

有任何问题，请在 GitHub 提交 Issue：<https://github.com/jiangwanyutao/bookmark-hub/issues>
